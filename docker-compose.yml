# docker-compose.yml
services:
  vergo_puppet:
    image: vergo_puppet
    container_name: vergo_puppet
    build: .
    ports:
      - "3004:8000"
    volumes:
      - .:/app
      - ./training_data:/app/training_data
      - ./test_data:/app/test_data